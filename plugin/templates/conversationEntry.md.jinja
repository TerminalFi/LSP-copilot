<style>
  html {
    --copilot-close-foreground: var(--foreground);
    --copilot-close-background: var(--background);
    --copilot-close-border: var(--foreground);
    --copilot-accept-foreground: var(--foreground);
    --copilot-accept-background: var(--background);
    --copilot-accept-border: var(--greenish);
  }

  .wrapper {
    margin: 1rem 0.5rem 0 0.5rem;
  }

  .wrapper .navbar {
    text-align: left;
  }

  .wrapper .header {
    display: block;
    margin-bottom: 1rem;
  }

  .wrapper a {
    border-radius: 3px;
    border-style: solid;
    border-width: 1px;
    display: inline;
    padding: 5px;
    text-decoration: none;
  }

  .wrapper a.close {
    background: var(--copilot-close-background);
    border-color: var(--copilot-close-border);
    color: var(--copilot-close-foreground);
  }

  .wrapper a.close i {
    color: var(--copilot-close-border);
  }

  .wrapper a.rating {
    background: var(--copilot-accept-background);
    border-color: var(--copilot-accept-border);
    color: var(--copilot-accept-foreground);
  }

  .wrapper a.rating i {
    color: var(--copilot-accept-border);
  }
</style>

<div class="wrapper">

<div class="navbar">
  <a class="close" title="Close Conversation" href='{{ close_url }}'><i>×</i> Close</a>&nbsp;
</div>

{% for section in sections %}
  <hr>
  <!-- This should really only be added to the entire response after streaming -->
  <div class="header">
    <a class="rating" title="👍" href='{{ section.accept_url }}'> 👍</a><a class="rating" title="👎" href='{{ section.accept_url }}'> 👎</a>
  </div>

<a class="copy_code" title="Copy Code" href='{{ section.accept_url }}'> ¶</a>
<a class="insert_code" title="Insert Code" href='{{ section.accept_url }}'> ¶</a>

{{ section.kind }} {{ section.content}}

{% endfor %}

</div>
